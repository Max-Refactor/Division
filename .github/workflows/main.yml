name: Testing
on: workflow_dispatch
jobs: 
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Get the repository code
        uses: actions/checkout@v3
      - name: Test
        uses: python3 division.py
